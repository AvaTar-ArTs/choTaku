# choTaku provider configuration is intentionally external to the semantic compiler.
# Add provider-specific variables only in local or deployment secret stores.
CHOTAKU_ENV=development
CHOTAKU_OUTPUT_DIR=build
